"""Fill mechanics: slippage, fees, order-book simulation, settlement.

Modules here may depend on ``domain`` and ``infrastructure`` and on the
``providers`` package, but never on ``orders`` or ``broker``.
"""
